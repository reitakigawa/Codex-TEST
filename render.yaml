services:
  - type: web
    name: codex-test-flask
    env: python
    plan: free
    rootDir: project
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
